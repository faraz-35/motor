// The JavaScript surface lives in the app (src/alarm/native.ts) so it resolves
// 'expo' from the app's node_modules. This package contributes the native
// Expo module named "MotorAlarms" via expo-module.config.json.
export {};
